{{#each carts_products}}
  <tr>
    <td>
      {{art_number}}
    </td>
    <td>
      {{name}}
    </td>
    <td>
      <div class="add-section">
        <a class="count-up" href="#" data-product-id="{{id}}"><span class="more">+</span>
        </a>
        <a class="count-down" href="#" data-product-id="{{id}}"><span class="less">-</span>
        </a>
        <input name="count" type="number" value="{{count}}" data-product-id="{{id}}">
      </div>
    </td>
    <td>
      {{price}}
    </td>
    <td>
      <a class="remove-product" data-product-id="{{id}}" rel="nofollow"
         href="/cart/{{id}}?product_id={{id}}">
        <span class="remove">
          Remove
        </span>
      </a>
    </td>
  </tr>
{{/each}}
<tr>
  <td>Total</td>
  <td></td>
  <td></td>
  <td>
    {{total}}
  </td>
  <td></td>
</tr>